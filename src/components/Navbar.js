import { useState } from "react";
import {
    createStyles,
} from "@mantine/core";

function link(desc, uri) {
	this.desc = desc;
	this.uri = uri;
}

export default function Navbar() {
    const [active, setActive] = useState(0);

		const items = links.map((l) => (
			<a>{link.l</a>
		))

}